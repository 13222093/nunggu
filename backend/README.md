# backend

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.3.5. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.

## Nabung Bareng (Feature Branch)

Initial spec and API stubs for pooled investing are available in [docs/nabung-bareng-spec.md](../docs/nabung-bareng-spec.md).

Router prefix: `/api/group` (see [backend/src/routers/group.ts](src/routers/group.ts)).
